import { color } from './color';
import { font } from './font';
import { layout } from './layout';
import { size } from './size';
import { zIndex } from './zIndex';

export const theme = { color, font, layout, size, zIndex };
